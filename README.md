# Raytracer running in a browser

## build

`npm run build`

You need to have Rust compiler and the newest version of `wasm-bindgen-cli` (but probably less than 0.3) (Can be installed with `cargo`). 
